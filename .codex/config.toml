
# Orlix beta archive and simulator validation need unsandboxed execution.
# Xcode/SwiftPM package resolution invokes Apple's sandbox-exec and fails inside
# Codex workspace sandboxing before the project build can start.
sandbox_mode = "danger-full-access"
approval_policy = "never"

[features]
hooks = true

[agents]
max_depth = 1
