---
name: orlix-bazel
description: Use for Orlix Bazel graph, action, cache, profile, BEP, or promoted-build work.
---
# Orlix Bazel

Read the active task envelope and [the Bazel procedure](references/procedure.md). Use Make as the repository interface and Bazel as the product graph. Keep upstream build engines authoritative inside their components. Record compact evidence references under `Build/AgentHarness/`.
