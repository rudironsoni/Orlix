diff --git a/options/posix/generic/posix_stdio.cpp b/options/posix/generic/posix_stdio.cpp
index 6353bc74..8e4659e3 100644
--- a/options/posix/generic/posix_stdio.cpp
+++ b/options/posix/generic/posix_stdio.cpp
@@ -233,7 +233,16 @@ int renameat(int olddirfd, const char *old_path, int newdirfd, const char *new_p
 	return 0;
 }
 
-int renameat2(int, const char *, int, const char *, unsigned int) {
-	__ensure(!"Not implemented");
-	__builtin_unreachable();
+int renameat2(int olddirfd, const char *old_path, int newdirfd, const char *new_path, unsigned int flags) {
+	if(flags) {
+		errno = ENOSYS;
+		return -1;
+	}
+
+	if(int e = mlibc::sysdep_or_enosys<Renameat>(olddirfd, old_path, newdirfd, new_path); e) {
+		errno = e;
+		return -1;
+	}
+
+	return 0;
 }
diff --git a/tests/meson.build b/tests/meson.build
index 4932c435..b19dca36 100644
--- a/tests/meson.build
+++ b/tests/meson.build
@@ -120,6 +120,7 @@ posix_test_cases = [
 	'posix/flockfile',
 	'posix/basename',
 	'posix/regex',
+	'posix/renameat2',
 	'posix/sigtimedwait',
 	'posix/if_indextoname',
 	'posix/readv-writev',
diff --git a/tests/posix/renameat2.c b/tests/posix/renameat2.c
new file mode 100644
index 00000000..7e833d82
--- /dev/null
+++ b/tests/posix/renameat2.c
@@ -0,0 +1,41 @@
+#ifndef _GNU_SOURCE
+#define _GNU_SOURCE
+#endif
+
+#include <assert.h>
+#include <errno.h>
+#include <fcntl.h>
+#include <stdio.h>
+#include <string.h>
+#include <unistd.h>
+
+int main(void) {
+	const char *old_path = "/tmp/mlibc-renameat2-old";
+	const char *new_path = "/tmp/mlibc-renameat2-new";
+
+	unlink(old_path);
+	unlink(new_path);
+
+	FILE *file = fopen(old_path, "w");
+	assert(file);
+	assert(fputs("renameat2\n", file) >= 0);
+	assert(!fclose(file));
+
+	assert(!renameat2(AT_FDCWD, old_path, AT_FDCWD, new_path, 0));
+
+	file = fopen(new_path, "r");
+	assert(file);
+	char buffer[32];
+	assert(fgets(buffer, sizeof(buffer), file));
+	assert(!strcmp(buffer, "renameat2\n"));
+	assert(!fclose(file));
+
+	errno = 0;
+	assert(renameat2(AT_FDCWD, new_path, AT_FDCWD, old_path, 0xFFFFFFFFu) == -1);
+	assert(errno == ENOSYS);
+
+	unlink(old_path);
+	unlink(new_path);
+
+	return 0;
+}
