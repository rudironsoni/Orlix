diff --git a/options/internal/include/mlibc/sysdep-tags.hpp b/options/internal/include/mlibc/sysdep-tags.hpp
--- a/options/internal/include/mlibc/sysdep-tags.hpp
+++ b/options/internal/include/mlibc/sysdep-tags.hpp
@@ -506,6 +506,8 @@ struct NameToHandleAt {};
 struct NameToHandleAt {};
 // int sys_splice(int in_fd, off_t *in_off, int out_fd, off_t *out_off, size_t size, unsigned int flags, ssize_t *out);
 struct Splice {};
+// int sys_vmsplice(int fd, const struct iovec *iov, size_t nr_segs, unsigned int flags, ssize_t *out);
+struct Vmsplice {};
 // int sys_shmat(void **seg_start, int shmid, const void *shmaddr, int shmflg);
 struct Shmat {};
 // int sys_shmctl(int *idx, int shmid, int cmd, struct shmid_ds *buf);
diff --git a/options/internal/include/mlibc/sysdep-signatures.hpp b/options/internal/include/mlibc/sysdep-signatures.hpp
--- a/options/internal/include/mlibc/sysdep-signatures.hpp
+++ b/options/internal/include/mlibc/sysdep-signatures.hpp
@@ -252,5 +252,6 @@ SYSDEP_FUNC(Waitid, idtype_t idtype, id_t id, siginfo_t *info, int options);
 SYSDEP_FUNC(NameToHandleAt, int dirfd, const char *pathname, struct file_handle *handle, int *mount_id, int flags);
 SYSDEP_FUNC(Splice, int in_fd, off_t *in_off, int out_fd, off_t *out_off, size_t size, unsigned int flags, ssize_t *out);
+SYSDEP_FUNC(Vmsplice, int fd, const struct iovec *iov, size_t nr_segs, unsigned int flags, ssize_t *out);
 SYSDEP_FUNC(Shmat, void **seg_start, int shmid, const void *shmaddr, int shmflg);
 SYSDEP_FUNC(Shmctl, int *idx, int shmid, int cmd, struct shmid_ds *buf);
 SYSDEP_FUNC(Shmdt, const void *shmaddr);
diff --git a/options/linux/generic/fcntl.cpp b/options/linux/generic/fcntl.cpp
--- a/options/linux/generic/fcntl.cpp
+++ b/options/linux/generic/fcntl.cpp
@@ -40,7 +40,11 @@ ssize_t splice(int in_fd, off_t *in_off, int out_fd, off_t *out_off, size_t size
 	return ret;
 }
 
-ssize_t vmsplice(int, const struct iovec *, size_t, unsigned int) {
-	__ensure(!"Not implemented");
-	__builtin_unreachable();
+ssize_t vmsplice(int fd, const struct iovec *iov, size_t nr_segs, unsigned int flags) {
+	ssize_t ret;
+	if(int e = mlibc::sysdep_or_enosys<Vmsplice>(fd, iov, nr_segs, flags, &ret); e) {
+		errno = e;
+		return -1;
+	}
+	return ret;
 }
diff --git a/sysdeps/linux/generic/sysdeps.cpp b/sysdeps/linux/generic/sysdeps.cpp
--- a/sysdeps/linux/generic/sysdeps.cpp
+++ b/sysdeps/linux/generic/sysdeps.cpp
@@ -2251,6 +2251,14 @@ int Sysdeps<Splice>::operator()(int in_fd, off_t *in_off, int out_fd, off_t *out
 	return 0;
 }
 
+int Sysdeps<Vmsplice>::operator()(int fd, const struct iovec *iov, size_t nr_segs, unsigned int flags, ssize_t *out) {
+	auto ret = do_syscall(SYS_vmsplice, fd, iov, nr_segs, flags);
+	if(int e = sc_error(ret); e)
+		return e;
+	*out = sc_int_result<ssize_t>(ret);
+	return 0;
+}
+
 int Sysdeps<Shmat>::operator()(void **seg_start, int shmid, const void *shmaddr, int shmflg) {
 	auto ret = do_syscall(SYS_shmat, shmid, shmaddr, shmflg);
 	if (int e = sc_error(ret); e)
diff --git a/sysdeps/linux/include/mlibc/sysdeps.hpp b/sysdeps/linux/include/mlibc/sysdeps.hpp
--- a/sysdeps/linux/include/mlibc/sysdeps.hpp
+++ b/sysdeps/linux/include/mlibc/sysdeps.hpp
@@ -161,6 +161,7 @@ using sysdeps = set<
 	Waitid,
 	NameToHandleAt,
 	Splice,
+	Vmsplice,
 	Shmat,
 	Shmctl,
 	Shmdt,
