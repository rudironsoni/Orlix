import OrlixNativeLinkProbe
import SwiftUI

struct NativeSmokePreview: View {
    var body: some View {
        Text(NativeLinkProbe.label)
    }
}

struct NativeSmokePreview_Previews: PreviewProvider {
    static var previews: some View {
        NativeSmokePreview()
    }
}
