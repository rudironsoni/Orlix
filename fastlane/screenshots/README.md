# App Store screenshots

Add reviewed App Store screenshots under a locale directory, for example `en-US/01-terminal.png`.

The production promotion gate fails until at least one PNG or JPEG screenshot is committed. Git LFS stores screenshot binaries.
