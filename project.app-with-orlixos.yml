targets:
  Orlix:
    dependencies:
      - target: OrlixOS
        embed: true
    settings:
      base:
        SWIFT_ACTIVE_COMPILATION_CONDITIONS:
          - $(inherited)
          - ORLIX_APP_WITH_ORLIXOS
  OrlixTestApp:
    dependencies:
      - target: OrlixOS
        embed: true
    settings:
      base:
        SWIFT_ACTIVE_COMPILATION_CONDITIONS:
          - $(inherited)
          - ORLIX_APP_WITH_ORLIXOS
